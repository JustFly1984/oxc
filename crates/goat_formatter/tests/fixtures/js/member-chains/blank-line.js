Promise.all(writeIconFiles)
  // TO DO -- END
  .then(() => writeRegistry())

Promise.all(writeIconFiles)

  // TO DO -- END
  .then(() => writeRegistry())

Promise.all(writeIconFiles)
  // TO DO -- END

  .then(() => writeRegistry())

// https://github.com/goat-project/oxc/issues/19469
const x = fn()

.c1();

const y = fn()

.c1()

.c2()


.c3();
