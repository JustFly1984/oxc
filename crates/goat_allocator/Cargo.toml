[package]
name = "goat_allocator"
version = "0.125.0"
authors.workspace = true
categories.workspace = true
edition.workspace = true
homepage.workspace = true
include = ["/src"]
keywords.workspace = true
license.workspace = true
publish = true
repository.workspace = true
rust-version.workspace = true
description.workspace = true

[lints]
workspace = true

[lib]
doctest = true

[dependencies]
goat_ast_macros = { workspace = true, optional = true }
goat_data_structures = { workspace = true, features = ["assert_unchecked", "stack"] }
goat_estree = { workspace = true, optional = true }

allocator-api2 = { workspace = true }
hashbrown = { workspace = true, default-features = false, features = [
  "inline-more",
  "allocator-api2",
] }
rustc-hash = { workspace = true }

serde = { workspace = true, optional = true }

[dev-dependencies]
goat_estree = { workspace = true, features = ["serialize"] }
serde = { workspace = true }
serde_json = { workspace = true }

# Enable `testing` feature in tests and doctests.
# Allows doctests to use `bump` module without exposing it publicly.
goat_allocator = { path = ".", features = ["testing"] }

[features]
bitset = []
fixed_size = ["from_raw_parts", "pool", "dep:goat_ast_macros"]
from_raw_parts = []
pool = []
serialize = ["dep:serde", "goat_estree/serialize"]
track_allocations = []
disable_track_allocations = []
# Only for use in tests
testing = []
