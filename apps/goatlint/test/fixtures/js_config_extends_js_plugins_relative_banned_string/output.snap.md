# Exit code
1

# stdout
```
Failed to parse goatlint configuration file.

  x Relative JS plugin specifiers are not supported in configs provided via `extends` in `goatlint.config.ts`.
  | 
  | Found: "./plugin.ts"
  | 
  | Use a package name (e.g. "eslint-plugin-foo") or an absolute path instead.
```

# stderr
```
```
